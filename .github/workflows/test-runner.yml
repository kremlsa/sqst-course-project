name: test-runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, Linux, X64]
    steps:
      - uses: actions/checkout@v4

      - name: Show env
        run: |
          uname -a
          whoami
          docker version
          pwd
