# requirements.txt — Зависимости учебного проекта
# ВНИМАНИЕ: намеренно содержит устаревшие версии с известными CVE
# для демонстрации анализа зависимостей (SCA) в уроке 9

# Flask 1.0.2 — CVE-2018-1000656: DoS через malformed JSON (HIGH)
flask==1.0.2

# Jinja2 2.10 — CVE-2019-10906: sandbox escape (HIGH)
jinja2==2.10

# Werkzeug 0.14.1 — CVE-2019-14806: predictable PRNG (MEDIUM)
werkzeug==0.14.1

# PyYAML 5.1 — CVE-2019-20477: arbitrary code execution (CRITICAL)
pyyaml==5.1

# requests 2.19.1 — CVE-2018-18074: credential leakage (MEDIUM)
requests==2.19.1
